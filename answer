❓ 问题 1：source=refined 时 max_len=196 还会不够吗？
会，但远没有我之前说的那么严重。 我重新核查了 refined cache：


refined news 长度统计 (字符):
  min=338, median=451, p75=467, p90=485, max=578
关键事实：

每篇 refined 文本 ≈ 450 字符 ≈ 110 tokens
单篇 refined 完全装得下 196 tokens
但是 _merge_refined_news_docs 会把每个 step 的 top_k=10 篇 refined 拼成一个字符串，然后 truncate 到 max_tokens=196
实际场景：每天 ~4 篇 → 4×110 = 440 tokens → 截到 196 → 只剩 ~1.7 篇 refined
结论：

不是"截断单篇文章"的问题（之前的 87% 是错的，那个数字针对的是 raw）
而是"merge 多篇 refined 时只保留最近 1-2 篇"
建议：把 DELTA_TEMPORAL_TEXT_MAX_LEN 从 196 提到 256 或 320，能装下 2-3 篇 refined。不需要去到 384 或 512。
❓ 问题 2：tail_tokens 改 head 还是 head+tail 拼接？
两个都不用做，因为 delta_news_tail_tokens=160 在你目前的管线里是死参数：


# src/model2.py:944-948
del (
    head_mlp,
    delta_news_tail_tokens,   # ← 直接 del 掉了
    delta_model_variant,
)
它只在已废弃的 raw-text-template 路径中用过，和 temporal_text 路径无关。我之前的"问题 1（取错了文章尾部）"在你 source=refined 的配置下是错的——refined cache 里存的就是 GPT 生成的整篇结构化摘要（"near-term impact direction: X. key drivers: [...]"），开头本身就是核心信号，根本不存在"头尾选哪个"的问题。

所以问题 1/2 实际只剩一个真问题：merge 多篇时 196 tokens 的预算不够。把 MAX_LEN 提到 256 即可。

❓ 问题 3：如何根除问题 5？
详细方案在 plan md 里，这里给一个一分钟版本：

问题 5 的真实严重程度
我重新算了一下冗余倍数：

层级	冗余原因	B1 是否覆盖
L1 sample 内 48 步共享同一批日级新闻	✅ B1 字符串去重把 48 → 2-5	
L2 同 batch 内相邻 sample 滑窗重叠 47/48 步	❌ truncation 顺序差异导致字符串哈希不命中	
L3 跨 batch / 跨 epoch 同一篇 doc 被无数次重新 encode	❌ 完全没覆盖	
真实冗余倍数：训练集 1166 篇 refined doc，但每个 epoch 实际 DistilBERT forward ~30000 次，冗余约 25 倍。

根治方案：Doc-Level Encoding（方案 A）
把"per-step merged text"作为编码单位，改成"per-doc"作为编码单位：


[一次性预处理 — 启动时跑一次]
1166 篇 refined doc → DistilBERT.encode → doc_vec_cache (1166×768 ≈ 3.5MB)
持久化到 checkpoints/_shared_refine_cache/doc_vec_cache_<dataset>.npz

[训练时]
对每个 step：
  1. 取 top-K 个 eligible doc 的 hash（沿用现有时间对齐逻辑）
  2. 从 doc_vec_cache 查向量 → [topk, 768]
  3. 用时间衰减权重做加权和 → [768]
  4. 经过现有的 self.proj → [target_dim=64]
→ 完全跳过 tokenize + DistilBERT forward
为什么这是"根治"而不是"缓解"
L1 自动消失：同一篇 doc 只编码一次，48 步共享
L2 自动消失：相邻 sample 共享的那 47 篇 doc 用同一份缓存
L3 自动消失：整个 epoch 只 forward 1166 次，下个 epoch 直接 load npz
收益
维度	当前	doc-level
epoch 时间（DELTA）	~5 min	~1 min（5-15x 加速）
DistilBERT forward 次数	~30000/epoch	1166（仅启动时一次）
信息保真度	受 truncation 顺序影响（拼接哈希不稳）	每篇 doc 独立完整编码
与 C3 协同	text_pack 因 truncation 抖动	text_pack 稳定
与 D1 协同	预训练后还得重新 forward	预训练后重新 dump npz 即可
兼容性
B2 text_shortcut_head：text_summary 仍然来自聚合后的 step_feat，无影响
C1 regime classifier：输入质量提升，受益
C3 cross-attention：text_pack 不抖动，受益
D1 contrastive pretrain：完美协同（pretrain → 重新生成 doc_vec_cache）
per_step_topk=10 / decay_tau_hours=24：仍然有效，只是从"merge 阶段使用"变成"聚合阶段使用"
实施成本
5 个文件改动，主要是新增 ~150 行代码
风险点处理：refined 缺失、refined 内容变化时缓存失效检测，plan 中已列出
不需要改 model 主干结构，只需要给 TemporalTextTower 加一个新方法 aggregate_from_doc_cache() 与 encode_unique_then_map() 并列
具体实施 7 步在 plan md 第七节，方案 B（解冻 encoder）和方案 C（配合 D1）作为后续增强路径列在第二节。